import "./Header.css"

// Crerando un componente
function Header(){
    return(
        <header className="header">
            <img src={"/img/Header.png"} alt="header org"></img>
        </header>
        )
}

// Tenemos que exportar el componente 
export default Header